1. Create Repo Locally & Push
mkdir myproject
cd myproject
git init
git add .
git commit -m "Initial commit"
git remote add origin URL
git branch -M main
git push -u origin main

2. Clone & Push Changes
git clone URL
cd project
git add .
git commit -m "Updated files"
git push origin main

3. Resolve Merge Conflict
git checkout main
git merge branch1
git merge branch2
# resolve conflict manually
git add .
git commit -m "Resolved merge conflict"
git push origin main

4. Pull Request
git checkout -b feature
git add .
git commit -m "Added feature"
git push origin feature
# Create PR on GitHub

5. Forking
Fork on GitHub
git clone URL
git checkout -b fix
git add .
git commit -m "Fix"
git push origin fix

6. Rebasing
git checkout feature
git fetch origin
git rebase origin/main
git push --force

7. Basic Commands
git init
git clone URL
git status
git add .
git commit -m "msg"
git pull origin main
git push origin main
git branch
git checkout branch
git merge branc